# BOARDinator

A JavaScript library for creating random board game boards

Install node, run "npm install" to install dependency modules then run:

  PORT=8080 node index.js

Or on Windows Powershell:

  $env:PORT=8080; node app.js

to start server.

Visit

  http://localhost:8080/boardinator.html

to view the website.
